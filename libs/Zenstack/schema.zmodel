import './schemas/models/user'
import './schemas/models/post'

datasource db {
    provider = 'postgresql'
    url = env("DB_URL")
}

plugin password {
    provider = './zenstack-plugins/generate-types.ts'
    report = true
}
